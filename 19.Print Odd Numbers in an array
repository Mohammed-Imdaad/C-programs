//Print odd numbers in an array
#include<stdio.h>
void main()
{
    int n,a[10000],i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
    if(a[i]%2!=0)
    {
        printf("%d ",a[i]);
    }
    }
}
