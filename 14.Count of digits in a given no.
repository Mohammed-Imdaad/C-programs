//Count of digits in a given number

#include<stdio.h>
void main()
{
    int n,c=0;
    scanf("%d",&n);
    while(n)
    {
        n=n/10;
        c++;
    }
    printf("Count of a given number is %d",c);
}
